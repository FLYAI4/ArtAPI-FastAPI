syntax = "proto3";

package pb;

message Request {
    bytes image = 1;
    string id = 2;
}

message Response {
    string tag = 1;
    bytes data = 2;
}

service StreamService {
    // Focus point method
    rpc GeneratedContentStream(Request) returns (stream Response) {}
}